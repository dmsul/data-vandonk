from setuptools import setup

setup(name='multisatpm',
      version='0.0.1',
      description='Easy access to multisatpm data.',
      url='http://github.com/dmsul/multisatpm',
      packages=['multisatpm']
      )
